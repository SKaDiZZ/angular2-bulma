import { BuIsFluidDirective } from './bu-is-fluid.directive';

describe('BuIsFluidDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsFluidDirective();
    expect(directive).toBeTruthy();
  });
});
