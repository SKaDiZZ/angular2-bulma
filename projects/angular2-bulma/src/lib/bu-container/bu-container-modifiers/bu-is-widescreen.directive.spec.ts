import { BuIsWidescreenDirective } from './bu-is-widescreen.directive';

describe('BuIsWidescreenDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsWidescreenDirective();
    expect(directive).toBeTruthy();
  });
});
