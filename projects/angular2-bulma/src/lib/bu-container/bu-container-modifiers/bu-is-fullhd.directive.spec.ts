import { BuIsFullhdDirective } from './bu-is-fullhd.directive';

describe('BuIsFullhdDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsFullhdDirective();
    expect(directive).toBeTruthy();
  });
});
