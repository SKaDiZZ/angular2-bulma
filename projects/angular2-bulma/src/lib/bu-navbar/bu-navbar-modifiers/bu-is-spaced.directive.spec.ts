import { BuIsSpacedDirective } from './bu-is-spaced.directive';

describe('BuIsSpacedDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsSpacedDirective();
    expect(directive).toBeTruthy();
  });
});
