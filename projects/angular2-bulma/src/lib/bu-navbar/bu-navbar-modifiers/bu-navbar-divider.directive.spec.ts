import { BuNavbarDividerDirective } from './bu-navbar-divider.directive';

describe('BuNavbarDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new BuNavbarDividerDirective();
    expect(directive).toBeTruthy();
  });
});
