import { BuHasShadowDirective } from './bu-has-shadow.directive';

describe('BuHasShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BuHasShadowDirective();
    expect(directive).toBeTruthy();
  });
});
