import { BuIsMobileDirective } from './bu-is-mobile.directive';

describe('BuIsMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsMobileDirective();
    expect(directive).toBeTruthy();
  });
});
