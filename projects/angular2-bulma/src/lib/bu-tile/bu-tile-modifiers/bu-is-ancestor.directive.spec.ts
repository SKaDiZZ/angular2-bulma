import { BuIsAncestorDirective } from './bu-is-ancestor.directive';

describe('BuIsAncestorDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsAncestorDirective();
    expect(directive).toBeTruthy();
  });
});
