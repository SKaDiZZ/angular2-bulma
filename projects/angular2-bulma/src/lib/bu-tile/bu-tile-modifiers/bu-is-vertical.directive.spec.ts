import { BuIsVerticalDirective } from './bu-is-vertical.directive';

describe('BuIsVerticalDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsVerticalDirective();
    expect(directive).toBeTruthy();
  });
});
