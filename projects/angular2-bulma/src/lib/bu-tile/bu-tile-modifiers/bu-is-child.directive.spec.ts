import { BuIsChildDirective } from './bu-is-child.directive';

describe('BuIsChildDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsChildDirective();
    expect(directive).toBeTruthy();
  });
});
