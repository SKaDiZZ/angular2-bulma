import { BuIsParentDirective } from './bu-is-parent.directive';

describe('BuIsParentDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsParentDirective();
    expect(directive).toBeTruthy();
  });
});
