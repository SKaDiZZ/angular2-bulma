import { BuIsSubmenuDirective } from './bu-is-submenu.directive';

describe('BuIsSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
