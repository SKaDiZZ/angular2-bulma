import { BuIsBoldDirective } from './bu-is-bold.directive';

describe('BuIsBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new BuIsBoldDirective();
    expect(directive).toBeTruthy();
  });
});
