import { BuHasFixedNavbarDirective } from './bu-has-fixed-navbar.directive';

describe('BuHasFixedNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new BuHasFixedNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
